# Decision records

One file per decision that a future reader would otherwise have to guess at.
Numbered, append-only: supersede a record with a new one rather than editing it.

Shape: **Context** (what forced a choice), **Decision** (what we did),
**Consequences** (what this costs us), **Alternatives** (what we rejected and
why). Short is good. Two hundred words is plenty.
